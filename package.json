{
	"name": "project-name-goes-here",
	"description": "The PROJECT-NAME-GOES-HERE project.",
	"version": "1.0.0",
	"scripts": {
		"preview": "open http://localhost:9090/",
		"serve": "npm run preview && cd dist/ && python -m SimpleHTTPServer 9090",
		"commit-checkpoint": "git add . && git commit -m 'dev: checkpoint' && git push",
		"clean": "rm -rf dist/*",
		"build": "npm run clean && gulp",
		"watch": "npm run build && gulp watch",
		"ftp": "cd dist/ && open . && cd /Applications && open Transmit.app",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"registry": "https://registry.npmjs.org/",
	"devDependencies": {
		"babel-core": "^6.26.3",
		"babel-polyfill": "^6.26.0",
		"babel-preset-env": "^1.7.0",
		"babel-preset-es2015": "^6.24.1",
		"babelify": "^8.0.0",
		"browserify": "^16.5.0",
		"gulp": "^3.9.1",
		"gulp-hb": "^8.0.0",
		"gulp-html-beautify": "^1.0.1",
		"gulp-load-plugins": "^2.0.2",
		"gulp-plumber": "^1.2.1",
		"gulp-rename": "^2.0.0",
		"gulp-sass": "^4.0.2",
		"gulp-uglify": "^3.0.2",
		"handlebars-layouts": "^3.1.4",
		"vinyl-buffer": "^1.0.1",
		"vinyl-source-stream": "^2.0.0"
	},
	"dependencies": {
		"@dogstudio/highway": "^2.2.1",
		"bootstrap": "^4.4.1",
		"gsap": "^3.5.1",
		"imagesloaded": "^4.1.4",
		"locomotive-scroll": "^4.0.6"
	}
}
